{% extends 'base-connected.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <div class="card">
                <div class="card-body">
                    <div class="card-subtitle mb-2 text-muted">My account</div>
                    <div class="card-title fs-4">{{ account.getBalanceOfMonth(date) }}</div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
